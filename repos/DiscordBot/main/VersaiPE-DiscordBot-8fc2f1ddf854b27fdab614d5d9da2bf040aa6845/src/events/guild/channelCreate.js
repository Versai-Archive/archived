const {categorys} = require("../../storage/config");
const {EmbedBuilder, Embed, ActionRowBuilder, ButtonBuilder} = require("discord.js");
const {interactions} = require("../../storage/interactions");

module.exports = async (client, channel) => {
    // none
}