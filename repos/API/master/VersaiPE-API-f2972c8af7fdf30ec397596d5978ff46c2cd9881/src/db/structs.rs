pub struct PlayerStats {
    pub ign: String,
    pub kills: u32,
    pub deaths: u32,
    pub kdr: f32,
    pub daily_kills: u32,
    pub daily_deaths: u32,
    pub daily_kdr: f32,
    pub monthly_kills: u32,
    pub monthly_deaths: u32,
    pub monthly_kdr: f32,
    pub ks: u32,
}
