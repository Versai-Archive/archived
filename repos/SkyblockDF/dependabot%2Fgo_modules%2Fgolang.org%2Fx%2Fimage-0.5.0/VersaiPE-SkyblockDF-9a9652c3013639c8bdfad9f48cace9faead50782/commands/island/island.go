package island

import (
	"github.com/df-mc/dragonfly/server/cmd"
)

type Island struct{}

func (t Island) Run(source cmd.Source, output *cmd.Output) {
	//p := source.(*player.Player)
	// TODO: Create an island for the player
}
